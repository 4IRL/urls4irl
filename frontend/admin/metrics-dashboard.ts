// Admin metrics dashboard controller. Real implementation lands in subsequent
// steps; this no-op exists so the entry-point bundle resolves and vite-build
// succeeds.
export function initMetricsDashboard(): void {
  // intentionally empty — populated by later steps
}
