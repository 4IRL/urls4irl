// Forward-reference stub for Step 2 (co-owner-transfer-ui plan). The real
// single-select member picker lands in Step 3; transfer.ts only needs the
// teardown export (closeTransferPicker) so its import resolves and every commit
// stays buildable. Do NOT add behavior here — Step 3 replaces this file.
export function closeTransferPicker(): void {}
