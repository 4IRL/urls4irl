# Folder-local conftest for `metrics_ui` Selenium tests.
#
# All session/parametrized fixtures (`browser`, `provide_app`, `runner`,
# etc.) are inherited from `tests/functional/conftest.py` and
# `tests/conftest.py` — no additional fixtures are required here.
