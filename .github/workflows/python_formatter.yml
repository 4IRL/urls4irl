name: Python Formatter

on:
  push:
    branches: [backend, frontend, dev, main]
  pull_request:
    branches: [backend, frontend, dev, main]
  
jobs:
<<<<<<< HEAD
<<<<<<< HEAD
  python-formatter-container:
=======
  build-test-container:
>>>>>>> 29629f2 (Adding Github Action Workflow for Python Formatter)
=======
  python-formatter-container:
>>>>>>> 2a8afb7 (fix naming on workflow jobs)
    # Containers must run in Linux based operating systems
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3
      with:
        ref: ${{ github.head_ref }}
        fetch-depth: 0
    - name: Check Python with Black
      uses: psf/black@stable
      with:
        options: " --verbose"       
    - name: Verify Python with Black
      uses: psf/black@stable
      with:
        options: "--check --verbose"
    - name: Check for modified files
      id: git-check
      run: echo "modified=$(if git diff-index --quiet HEAD --; then echo "false"; else echo "true"; fi)" >> $GITHUB_OUTPUT
    - name: Commit Lint Changes
<<<<<<< HEAD
      if: steps.git-check.outputs.modified == 'true'
      run: |
        git config --local user.email "${{ secrets.MY_EMAIL }}"
        git config --local user.name "{{ secrets.NAME }}"
        git commit -a -m "Formatted Python code - Black"
    - name: Push changes
      if: steps.git-check.outputs.modified == 'true'
=======
      run: |
        git config --local user.email "${{ secrets.MY_EMAIL }}"
        git config --local user.name "{{ secrets.NAME }}"
        git commit -a -m "Add changes"
    - name: Push changes
>>>>>>> 29629f2 (Adding Github Action Workflow for Python Formatter)
      uses: ad-m/github-push-action@master
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        branch: ${{ github.head_ref }}
